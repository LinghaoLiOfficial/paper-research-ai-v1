from config.name.BaseName import BaseName


class AnalysisParamsEnName(BaseName):
    EPOCHS = "epochs"
    LR = "lr"
    TRAIN_RATIO = "train_ratio"
    BATCH_SIZE = "batch_size"
    PREDICT_RANGE = "predict_range"
    RANDOM_SEED = "random_seed"
    TIMESTEP = "timestep"
    WEIGHT_DECAY = "weight_decay"

    